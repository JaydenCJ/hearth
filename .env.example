# Hearth environment configuration.
# Copy to .env next to docker-compose.yml; docker compose picks it up.

# Path of the hub configuration file inside the container. The default
# points at the named volume mounted on /config; drop your hearth.yaml
# there (see server/examples/hearth.example.yaml) to leave mock mode.
HEARTH_CONFIG=/config/hearth.yaml
